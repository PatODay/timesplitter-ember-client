<nav class="navbar navbar-default">
  <div class="container-fluid">
    {{navbar-header}}

    <div class="collapse navbar-collapse" id="navigation">
      <ul class="nav navbar-nav navbar-right">
        {{#if isAuthenticated}}
        <li>{{#link-to "create-task"}}Create Task{{/link-to}}</li>
        <li>{{#link-to "change-password"}}Change Password{{/link-to}}</li>
        <li><a href="#" {{action "signOut"}}>Sign Out</a></li>
        {{else}}
        <li>{{#link-to "sign-up"}}Sign Up{{/link-to}}</li>
        <li>{{#link-to "sign-in"}}Sign In{{/link-to}}</li>
        {{/if}}
      </ul>
    </div>
  </div>
</nav>
{{#if isAuthenticated}}
<h1 class="header">Welcome to TimeSplitter, {{user}}!</h1>
{{!-- <h3>What would you like to do?</h3>
<button class="create-task">{{#link-to "create-task"}}Create Task{{/link-to}}</button>
<button class="view-tasks">{{#link-to "tasks"}}Tasks{{/link-to}}</button> --}}
{{else}}
<h1 class="no-auth-header">Welcome to TimeSplitter!</h1>
{{/if}}

{{#each flashMessages.queue as |flash|}}
  {{flash-message flash=flash}}
{{/each}}

<div class="col-md-8 col-md-offset-2">
  {{outlet}}
</div>
